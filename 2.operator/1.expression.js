// operators(연산자)
// literal  -> 코드에서 값을 나타내는 표기법  ex 123 , '123', true, {}, []

1      // 숫자 리터럴 표현식
1 + 1  // 연산자 표현식
call() // 함수 호출 표현식
let b; // 선언문
b = 2; // 표현식, 할당문

let a = (b = 2);
console.log(a); // 2
